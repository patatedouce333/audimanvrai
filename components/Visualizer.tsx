
import React from 'react';

const Visualizer: React.FC<{ isActive: boolean; volume: number; isMuted?: boolean }> = () => {
  return null; // Suppression totale du composant visuel encombrant
};

export default Visualizer;
